mixin menu(className, data)
  ul(class=`${className}`)
    each item, index in data
      li(class=`${className}__item` + ' ' +((index === 1 && className === "menu") ? `${className}__item--active` : ''))
        a(class=`${className}__link` href='#')= item
